interface:
  display_name: "Redesign From First Principles"
  short_description: "Apply when integrating a new requirement into an existing design"
  default_prompt: "Use $principle-redesign-from-first-principles to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
