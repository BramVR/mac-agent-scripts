interface:
  display_name: "Unslop"
  short_description: "Remove AI tells and restore human voice"
  default_prompt: "Use $unslop to rewrite this text without AI tells while preserving its meaning and intended tone."
policy:
  allow_implicit_invocation: false
