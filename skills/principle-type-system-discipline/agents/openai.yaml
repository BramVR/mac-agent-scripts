interface:
  display_name: "Type System Discipline"
  short_description: "Apply when designing types, reviewing a function signature, or writing code in any statically-typed language"
  default_prompt: "Use $principle-type-system-discipline to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
