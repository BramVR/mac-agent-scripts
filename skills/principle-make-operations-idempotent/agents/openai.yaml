interface:
  display_name: "Make Operations Idempotent"
  short_description: "Apply when designing commands, lifecycle steps, or processing loops that run amid crashes, restarts, and retries"
  default_prompt: "Use $principle-make-operations-idempotent to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
