interface:
  display_name: "GitHub Cache Hygiene"
  short_description: "GitHub live-first cache fallback"
  default_prompt: "Use $github-cache-hygiene to keep GitHub reads live-first, warm gitcrawl for supported reads, and use cached/offline fallback only for outages, rate limits, or explicit offline requests."
