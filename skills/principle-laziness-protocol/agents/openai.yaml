interface:
  display_name: "Laziness Protocol"
  short_description: "Apply when refactoring, evaluating diff size, or tempted to add abstractions, layers, or signal threading"
  default_prompt: "Use $principle-laziness-protocol to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
