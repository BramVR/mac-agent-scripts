interface:
  display_name: "Bram Maintainer Loop"
  short_description: "Coordinate flagged repo maintenance loops"
  default_prompt: "Use $bram-maintainer-loop to triage Bram's flagged repos, create a coordinator heartbeat when authorized, delegate workers that use $github-project-triage as the issue/PR workhorse, and merge prepared PRs when granted after TDD/tests, live proof, CI, confidentiality gate, and autoreview."
