interface:
  display_name: "Minimize Reader Load"
  short_description: "Apply when reviewing or shaping code that's hard to trace"
  default_prompt: "Use $principle-minimize-reader-load to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
