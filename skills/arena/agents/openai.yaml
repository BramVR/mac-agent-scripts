interface:
  display_name: "Arena"
  short_description: "Race candidates, judge them, and synthesize one"
  default_prompt: "Use $arena to generate competing solutions, select a base, graft the strongest ideas, and verify it."
policy:
  allow_implicit_invocation: false
