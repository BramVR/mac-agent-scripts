interface:
  display_name: "Swarm"
  short_description: "Fan out parallel workers and consolidate results"
  default_prompt: "Use $swarm to fan this task out across parallel workers and return one consolidated report."
policy:
  allow_implicit_invocation: false
