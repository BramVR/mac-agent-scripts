interface:
  display_name: "Never Block On The Human"
  short_description: "Apply when tempted to ask 'should I do X?' on reversible work"
  default_prompt: "Use $principle-never-block-on-the-human to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
