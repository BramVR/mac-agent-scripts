interface:
  display_name: "Build the Lever"
  short_description: "Turn non-trivial work into a rerunnable tool"
  default_prompt: "Use $principle-build-the-lever to create the smallest rerunnable tool for this work."
policy:
  allow_implicit_invocation: false
