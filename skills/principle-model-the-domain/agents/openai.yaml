interface:
  display_name: "Model The Domain"
  short_description: "Apply when writing stateful logic, or when code branches a lot or repeats a shape assumption across files"
  default_prompt: "Use $principle-model-the-domain to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
