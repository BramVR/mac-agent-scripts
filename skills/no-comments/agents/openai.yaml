interface:
  display_name: "No Comments"
  short_description: "Remove weak comments and encode real constraints"
  default_prompt: "Use $no-comments to audit these comments, remove weak ones, and encode real constraints."
policy:
  allow_implicit_invocation: false
