interface:
  display_name: "Separate Before Serializing Shared State"
  short_description: "Apply when concurrent actors might write to the same file, branch, key, or state object"
  default_prompt: "Use $principle-separate-before-serializing-shared-state to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
