interface:
  display_name: "Outcome Oriented Execution"
  short_description: "Apply during planned rewrites and migrations with explicit phase boundaries"
  default_prompt: "Use $principle-outcome-oriented-execution to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
