interface:
  display_name: "Migrate Callers Then Delete Legacy Apis"
  short_description: "Apply when introducing a new internal API while old callers still exist"
  default_prompt: "Use $principle-migrate-callers-then-delete-legacy-apis to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
