interface:
  display_name: "Encode Lessons In Structure"
  short_description: "Apply when you catch yourself writing the same instruction a second time, or notice a recurring correction"
  default_prompt: "Use $principle-encode-lessons-in-structure to apply this principle to the current decision."
policy:
  allow_implicit_invocation: false
