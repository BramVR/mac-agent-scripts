# Upstream mining marker

- Reviewed `upstream/main` through: `6878dd818b34659a925ec45fb7225a81b6a5c69a` (2026-07-22, reviewed 2026-07-23)
- Full classification report of the 232-commit pass (through `0798fed`): 19 groups already present, 12 skip (Peter-personal), 19 port candidates, 2 preserved-skill conflict families (autoreview, maintainer-loop). Port candidates pending Bram triage.
- 2026-07-23 pass (`0798fed..6878dd8`, 1 commit): ported codex-first git-mechanics mandate, guarded CI waits, fresh work-order sessions, AGENTS.md-only rule; scrubbed openclaw watcher example + CLAUDE.md-symlink claim; kept Bram divergences (no fast_mode, loopback-only gate, short description, `$bram-maintainer-loop-v2` pointer).
- 2026-08-19 targeted refresh: mined `skills/browser-use` through `2e320ff086cfc82d01037edab0683857d48c1698`; combined current relay hardening with Bram's cmux-first route. Global marker unchanged because this was not a full upstream classification pass.
- 2026-09-03 targeted PStack pass: mined `cursor/plugins` `pstack` at `b9ddc83c32972210b8a94d389130713e8eed346e`; ported 21 principle skills, Poteto Mode, and supporting workflows with Codex collaboration, permission, transcript, and model-routing adaptations. Global marker unchanged because this was a separate upstream.
- 2026-09-08 targeted PStack refresh: reconciled `cursor/plugins` `pstack` through `23a56e2dac2efd54788056db8eced26e371d7b5e` (including `73f8be4`). The existing Codex playbooks already contained the applicable forge-neutral PR, stack, regression-lane, and patch-id wording. Omitted Cursor-only `paths` and `disable-model-invocation` frontmatter because the Codex skill validator rejects those keys. Adapted upstream Fable 5.1 routing to the local role policy: Astra medium for judgment and primary review, optional Fable 5.1 max for a second review, Sol high for implementation, Luna high for discovery and procedural verification. Added `typescript-best-practices` with its upstream patterns and added a fail-closed Codex adaptation of `make-bot-ui`. Skipped Cursor plugin manifest and guide changes plus `setup-pstack`; the latter installs Cursor-specific model rules and has no Codex counterpart.
- 2026-09-13 PStack bump: regenerated at `cursor/plugins` `889ec4b` (bug-fix, perf-issue, hillclimb on the fast code model upstream, `gpt-5.6-sol` here). Mined `michael-denyer/pstack-claude` v0.9.29 for its hardened `poteto-mode/scripts` (watch-pr pagination and head-SHA integrity, serialized orch store, NUL-safe worktree audit) and adopted six `cursor-team-kit` skills it imports (`fix-ci`, `fix-merge-conflicts`, `get-pr-comments`, `make-pr-easy-to-review`, `thermo-nuclear-code-quality-review`, `what-did-i-get-done`). Not adopted from that port: its Claude-native skill bodies, `codex-tools.md` runtime mapping, standalone `babysit` skill, merge-ready-only autopilots, and Claude-only interrogate panel.
- 2026-09-14 PStack setup refresh: ported `cursor/plugins` `pstack` through `5bf2b1544d` (reasoning-budget choice in `setup-pstack`), adapting upstream model-slug effort changes to Codex's separate `model` and `reasoning_effort` fields. Global marker unchanged because this was a separate upstream.
- 2026-09-12 PStack regeneration: mined `cursor/plugins` `pstack` at `f5bdd68d`; every ported skill, playbook, reference, and script is now generated from upstream by `port-upstream.py` (minimal Codex substitutions, kept-ours list for environment rules, privacy appends for the `why` source playbooks). Ported the last upstream-only skills (`automate-me`, `setup-pstack`) and the `cursor-team-kit` `control-cli`/`control-ui`. Still upstream wording, awaiting a decision: Bugbot, Origin, Graphite, `/loop` as a periodic tick, and the `why` MCP sources Bram does not run.

## Flow (repeat per pass)

```bash
cd ~/Projects/agent-scripts && git fetch upstream
git log --reverse <marker-sha>..upstream/main
```

Delegate classification to Codex ($codex-first): buckets = already-present / Peter-personal skip / port candidate / touches preserved skills (never blind-port those). Port selectively via /tmp staging (AGENTS.MD rule). Practice-revealing changes also go to the vault as claims on `Sources/2026-07-22 steipete agent-scripts.md` (SHA = evidence, commit date = published). Update the marker SHA here after each pass.
